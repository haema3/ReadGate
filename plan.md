# ReadGate: 2시간 해커톤 사이트·데모 계획

- 작성일: 2026-09-19
- 상태: P0 초기 구현 완료. 로컬 사이트·SDK·서버 채점·독립 HTML 예제·컨테이너 정의·플랫폼 준비 스크립트가 있으며, Daytona·Nosana의 실제 원격 실행은 자격 증명과 비용 승인 후 수행한다.
- 제품명: ReadGate는 데모용 가칭이다.
- 기준: [AGENTS.md](AGENTS.md)의 Daytona 개발·검증 / Nosana GPU·AI 실행 분리를 따른다.

## 1. 해커톤 목표와 성공 기준

**서비스 설명:** 웹 페이지에 스크립트 한 줄을 연결하면 스크롤·마우스·체류 시간으로 놓쳤을 가능성이 있는 문단을 찾고, 해당 문단의 퀴즈에 정답을 제출해야 다음 단계로 진행하게 하는 임베더블 SDK.

2시간의 목표는 상용 SaaS 완성이 아니라, 심사자가 서비스의 가치를 이해하고 직접 체험하며 두 플랫폼의 실제 사용을 확인할 수 있는 사이트다.

- 설명: 첫 화면에서 무엇을 해결하는 서비스인지 이해할 수 있다.
- 체험: 고정된 글을 빠르게 넘기면 문단에 맞는 문제가 나오고, 오답에는 잠금이 유지되며 정답 후 다음 화면이 열린다.
- 임베딩: 사이트와 별도의 단순 HTML 예제에서 동일한 SDK 스크립트가 동작한다.
- Daytona: 실제 샌드박스에서 SDK 예제 실행·테스트를 수행하고 결과를 남긴다.
- Nosana: 실제 GPU 작업에서 데모 본문 기반 문제를 생성하고 그 결과를 퀴즈에 사용한다.

**행동 신호는 읽기 여부의 추정치이지 실제 이해도나 시선의 증거가 아니다.** 또한 플랫폼 로고나 모의 성공 화면만으로 실제 연동을 완료했다고 표현하지 않는다. 배점과 제출 요건은 대회 규정을 확인하며, 이 계획이 점수를 보장하지는 않는다.

## 2. 사이트 구성

### 한 페이지에서 설명과 체험 연결

| 영역 | 내용 | 완료 기준 |
| --- | --- | --- |
| 첫 화면 | 큰 제품명 `ReadGate`, 짧은 서비스 설명, `체험 시작`·`설치 코드` 이동 버튼 | 과도한 소개 화면 없이 데모 본문 일부가 첫 화면에 보인다. |
| 체험 데모 | 3개 문단의 짧은 안내 글, 다음 단계 버튼, 문단별 관찰 상태 | 심사자가 직접 스크롤하고 퀴즈를 풀 수 있다. |
| 퀴즈 및 결과 | 대상 문단, 객관식 문제, 제출 결과, 다시 읽기, 완료 화면 | 오답과 정답의 진행 차이가 실제로 동작한다. |
| 서비스 설명 | 기존 페이지에 SDK 연결 → 놓친 문단 추정 → 퀴즈로 확인 | 교육·온보딩에서의 사용 가치를 짧게 전달한다. |
| 한 줄 설치 | 실제 SDK 주소를 이용한 코드, 복사 버튼, 별도 HTML 예제 링크 | 가짜 CDN 주소가 아니라 빌드한 파일을 연결한다. |
| 플랫폼 증빙 | Daytona 테스트 결과, Nosana 생성 출처, 실행 시각과 상태 | 실제 기록이 없는 서비스는 `미연동`으로 표시한다. |

서비스 소개 문구 초안: "넘긴 문단을, 이해한 문단으로. 스크롤·체류 신호로 놓친 내용을 찾아 짧은 퀴즈로 확인합니다." 제품명은 제목으로, 이 문구는 보조 설명으로 사용한다.

### 화면과 상호작용

- 데스크톱은 본문과 좁은 관찰 패널, 모바일은 본문 아래 요약으로 구성한다. 섹션 전체를 카드로 감싸거나 카드를 중첩하지 않는다.
- 밝은 바탕·차콜 텍스트를 기본으로 하고 읽기 상태에만 초록·주황 계열 강조색을 쓴다. 한글 가독성과 안정적인 글줄 길이를 우선한다.
- 문단별 `관찰 중 / 확인 필요 / 확인 완료`와 유효 체류 시간을 보여준다. 이를 "이해도 95%" 같은 확정적 수치로 포장하지 않는다.
- 퀴즈는 읽기를 방해하는 자동 팝업 대신 다음 단계 요청 시 열린다. 키보드 제출, 상태 알림, 초점 복귀를 지원한다.
- 시각 자료는 실제 동작 화면과 플랫폼 실행 결과 캡처를 사용한다. 장식용 3D·동영상 배경·복잡한 애니메이션은 만들지 않는다.
- 초기 로딩, 문제 준비 중, 오답, 정답, 네트워크 오류, 미연동 상태를 빠짐없이 구분한다.

## 3. 데모 흐름과 SDK 경계

### 고정 시나리오

1. 심사자가 `체험 시작`을 누른다. 필요한 고지·동의 뒤 관찰을 시작하며, 원문은 언제든 읽을 수 있다.
2. 직접 작성한 "해커톤 참가 안내" 3개 문단을 보여준다. 문단 ID와 콘텐츠 버전은 고정하고 문단당 문제 1개를 준비한다.
3. 일부 문단을 빠르게 넘긴 뒤 `다음 단계`를 누르면 유효 체류가 부족하거나 판단할 수 없는 문단을 대상으로 출제한다.
4. 문제를 한 개씩 제시하고 오답이면 해당 문단으로 돌아가 다시 읽을 수 있게 한다. 다시 읽는 것만으로 이미 출제된 문제를 면제하지 않는다.
5. 현재 출제 대상 문제를 모두 맞히면 완료 화면으로 이동한다. 대상이 없으면 퀴즈 없이 통과한다.
6. `다시 체험`은 세션·타이머·출제 상태를 초기화한다. 발표자가 숨은 조작으로 결과를 바꾸는 기능은 넣지 않는다.

### 최소 읽기 판정

- `IntersectionObserver`와 Page Visibility, 창 포커스로 문단 노출 중 유효 시간을 집계한다. 숨김 탭 시간은 제외한다.
- 스크롤 속도와 문단 통과 여부를 보조 근거로 기록한다. 텍스트 길이에 따른 체류 기준과 노출 여부로 판정하는 규칙부터 만든다.
- 마우스 활동은 약한 보조 신호로만 표시한다. 마우스가 없거나 움직이지 않는다고 감점하지 않고, 마우스를 흔드는 것만으로 통과하지 않는다.
- 기본 분류는 `충분한 신호 / 확인 필요 / 판단 불가`다. 판단 불가는 자동 통과시키지 않고 퀴즈 대상으로 포함한다.
- 짧은 데모 글에 맞춘 임계값임을 명시한다. 장문·모든 언어에 대한 정확도나 검증되지 않은 탐지 성능은 주장하지 않는다.
- 원시 좌표·키 입력 내용은 저장하지 않는다. 서버에는 문단 ID와 필요한 신호 요약만 전달한다.

### 한 줄 임베딩

아래 코드는 같은 출처에서 실행할 데모의 예정 계약이다. 아직 SDK 파일이 구현되어 있다는 의미는 아니다.

```html
<script defer src="/sdk.js" data-target="#article" data-next="#continue" data-content-id="hackathon-guide-v1"></script>
```

- 서비스 사이트도 별도 HTML 예제도 동일한 SDK 산출물을 사용한다. 사이트 전용 데모를 SDK인 것처럼 보여주지 않는다.
- 설치 코드를 표시할 때는 실행 중인 서버의 실제 주소를 사용한다. 2시간 범위는 같은 출처의 정적 본문·명시적인 다음 버튼으로 한정한다.
- 일반 `textarea`에 텍스트로 붙이거나 스크립트를 제거하는 편집기에서는 실행되지 않는다. 스크립트 실행이 허용된 HTML 영역이 필요하다.
- 별도 도메인의 CORS·CSP·인증 연동과 모든 SPA 라우터 지원은 후속 작업이다. 임의 사이트에서 즉시 동작한다고 주장하지 않는다.

## 4. 2시간 구현 범위

### P0: 발표에 반드시 필요한 것

- 서비스 설명과 실제 체험이 연결된 반응형 한 페이지.
- 고정 본문 3개 문단, 문단별 신호, 객관식 퀴즈, 오답 잠금, 정답 후 완료, 다시 체험.
- 재사용 가능한 SDK 파일과 별도 HTML 예제 1개.
- 작은 서버의 세션·문제 제공·채점·완료 확인. 정답은 프런트엔드 번들에 넣지 않는다.
- Daytona에서 실제 예제 실행과 핵심 테스트 1회 이상 성공.
- Nosana에서 실제 추론 1회 이상 성공하고 검수한 생성 문항을 데모에 연결.
- 실제 실행 증빙, 명확한 미연동·재사용 표시, 발표용 스크린샷 또는 짧은 녹화.

### P1: P0가 끝난 경우에만

- 문단 상태의 짧은 전환 효과, 설치 코드 복사 피드백, 발표용 요약 수치.
- 추가 브라우저 검증과 한 개의 다른 고정 본문 예제.

### 이번에는 만들지 않는 것

- 로그인·결제·관리자 대시보드·다중 고객 설정·별도 DB·완료 토큰 외부 연동.
- 임의 URL 수집, 임의 글 업로드, 매 방문자 실시간 AI 생성, 모델 학습·파인튜닝.
- 문단 자동 그룹화, 동적 문서 변경 감지, PDF·영상·교차 출처 iframe, 범용 SPA 플러그인.
- 복잡한 ML 읽기 판정, 개인화, 부정행위 방지 보증, 법적 동의 증명.

상용 수준의 인증·외부 서비스 접근 제한은 후속 범위다. 데모의 서버 채점은 정답 노출과 단순 클라이언트 상태 변경을 줄이지만, 조작된 행동 신호나 실제 이해도까지 증명하지는 않는다.

## 5. Daytona·Nosana 실사용과 심사 증빙

두 플랫폼은 막판에 로고를 붙이는 항목이 아니라 P0 의존성이다. 계정·크레딧·이미지·가용량을 먼저 확인하고, 원격 작업을 시작한 뒤 대기 시간에 사이트를 구현한다.

### Daytona: 격리된 SDK 실행·검증

1. 공식 SDK로 최소 자원의 임시 샌드박스를 만들고 예제·SDK·테스트를 전달한다.
2. 실제 빌드와 테스트, 독립 HTML 예제 및 컨테이너 스모크 테스트를 실행한다. 샌드박스 생성 성공만으로 완료 처리하지 않는다.
3. 실행 시각, 공개 가능한 샌드박스 식별자, 실행한 명령, 종료 코드와 결과 요약을 외부 작업 공간으로 내보낸다.
4. 사이트 증빙 영역에는 실제 실행 결과와 캡처를 표시한다. 지원되는 경우 미리보기를 제공하되 Daytona를 영구 운영 호스팅으로 가정하지 않는다.
5. 유한 TTL과 비활성 종료를 설정하고 `finally`로 정리를 보장한다. 발표 중 재사용이 필요하면 보존 시간을 명시하고 발표 후 삭제한다.

### Nosana: 본문 기반 AI 퀴즈 생성

1. 현재 공식 문서·작업 스키마와 가용 시장을 확인하고, 요구 자원이 확인된 준비된 추론 이미지·모델을 우선 사용한다. 처음부터 모델을 학습하거나 대형 이미지를 새로 설계하지 않는다.
2. 비용 승인 후 실제 GPU 작업을 시작하고 제한된 시간 안에 준비 상태와 추론 엔드포인트를 확인한다.
3. 서버 또는 운영 스크립트가 승인된 3개 문단을 전달해 문단당 문제 1개를 생성한다. 공개 방문자가 새 GPU 작업을 생성하는 API는 만들지 않는다.
4. 문항 구조, 선택지, 단일 정답, 근거 문단, 해설을 검증하고 3문항은 사람이 확인한다. 본문의 지시문은 모델 실행 지시가 아닌 데이터로 취급한다.
5. 검수된 결과를 콘텐츠 버전·입력 해시·생성 시각·실행 식별자와 함께 외부에 보존하고 실제 퀴즈 API에서 사용한다. 잘못된 결과가 기존 검수 문항을 덮어쓰지 않게 한다.
6. 발표에서는 이 실제 생성 결과를 재사용해 GPU 시작 지연을 피한다. 재사용임을 표시하고, 대회가 실시간 호출을 요구하면 준비된 엔드포인트를 통한 추가 호출을 시연한다.
7. 실행 목적이 끝나면 작업을 종료한다. 실시간 시연을 위해 유지하는 경우에도 예산·최대 유지 시간을 정한다.

### 제출 증빙

| 플랫폼 | 최소 증빙 | 제품과의 연결 |
| --- | --- | --- |
| Daytona | 실제 샌드박스 실행 기록, 성공한 SDK 테스트, 예제 화면 | 기존 페이지와 분리된 환경에서도 같은 SDK가 실행됨을 보여준다. |
| Nosana | 실제 작업 식별자, 준비·추론 결과, 생성 문항과 콘텐츠 연결 | 심사자가 푸는 문제가 해당 GPU 실행에서 생성되었음을 보여준다. |

- 증빙에는 현재 실행 상태와 마지막 성공 시각을 구분한다. 종료된 작업을 `현재 연결됨`으로 표시하지 않는다.
- 캡처와 로그에서 API 키·토큰·계정 정보·비공개 엔드포인트를 제거한다. 증빙은 샌드박스나 GPU 작업이 삭제되어도 남아야 한다.
- 단순 목업, 다른 공급자의 응답, 직접 작성한 문제를 Daytona·Nosana 실행 결과로 표시하지 않는다.

## 6. 최소 기술 구성과 안전장치

기존 기술 스택이 없으므로 아래는 구현 시작 시 확정할 권장안이며, 아직 도입한 상태는 아니다.

| 구성 | 최소 선택 |
| --- | --- |
| 사이트 | TypeScript와 Vite의 바닐라 구성 권장. 프런트엔드 프레임워크·라우터는 추가하지 않는다. |
| SDK | 네이티브 DOM API를 사용하는 독립 JavaScript 산출물. 사이트 UI와 읽기 판정을 분리한다. |
| 서버 | 작은 Node.js 서버 하나가 사이트·SDK·문제·채점 API를 같은 출처에서 제공하는 구성 권장. |
| 세션 | 만료가 있는 서버 메모리 상태. 재시작하면 초기화되는 데모 한계를 명시한다. |
| 문항·증빙 | 검수된 정적 데이터와 실행 기록을 외부 작업 공간에 보존. 별도 DB는 두지 않는다. |
| 공급자 연동 | 공식 SDK를 사용하는 작은 Daytona·Nosana 모듈. 도메인 코드에서 분리하며 API 이름·버전은 공식 문서 확인 후 고정한다. |
| 사이트 공개 | 이용 가능한 CPU 호스팅 또는 승인된 임시 미리보기. 비용·공개 범위는 먼저 확인한다. |

### 생략하지 않을 것

- 서버는 고정된 콘텐츠와 문항 ID, 입력 크기, 세션, 답변을 검증한다. 생성된 문제는 HTML이 아닌 텍스트로 렌더링한다.
- 출제 집합을 서버 세션에 고정하고 모든 필수 정답을 확인한 뒤 데모의 다음 단계 요청을 허용한다. 클라이언트의 `passed` 값은 신뢰하지 않는다.
- 정답을 포함한 문항 원본은 공개 정적 디렉터리 밖에 둔다. 브라우저에는 질문·선택지와 제출 후 필요한 피드백만 전달한다.
- 요청에는 제한된 대기·속도 제한을 적용하고 일시 실패만 제한적으로 재시도한다. 중복 제출·생성·정리 요청을 안전하게 처리한다.
- SDK 중복 초기화를 막고 다시 체험·해제 시 리스너와 타이머를 정리한다. 원문 열람은 잠그지 않는다.
- 서버 채점이나 네트워크가 실패해도 자동 통과시키지 않는다. SDK 미로딩 시에도 보호된 다음 단계는 서버에서 거부한다.
- `DAYTONA_API_KEY`, `DAYTONA_API_URL`, `DAYTONA_TARGET`, `NOSANA_API_KEY`는 서버·운영 환경에서만 사용한다. 실제 키를 채팅·브라우저·이미지·커밋·로그에 넣지 않는다.
- 의존성과 실행 이미지 버전을 고정하고, 필요한 환경 변수 예시·다단계 컨테이너 빌드·상태 확인·종료 처리·실행 및 정리 안내를 함께 제공한다.
- Nosana 작업 정의는 `deploy/nosana/`의 버전 관리 JSON으로 두고 현재 스키마로 검증한다. 이미지·명령·환경·자원·서비스·상태 확인을 명시한다.
- 배포할 워크로드는 동일하게 고정된 이미지로 로컬 또는 Daytona에서 검증한다. GPU가 필요한 검증을 CPU 스모크 결과만으로 통과했다고 주장하지 않는다.
- [Daytona 공식 문서](https://www.daytona.io/docs/)와 [Nosana 공식 문서](https://learn.nosana.com/)에서 현재 API·스키마를 확인한 후 구현한다. 이 계획 작성 단계에서는 이를 검증 완료로 간주하지 않는다.

## 7. 120분 구현 타임박스

한 명이 구현하는 기준이다. 원격 준비 작업의 대기 시간은 겹쳐 사용하되, 별도 인력이나 에이전트 병렬 작업을 전제하지 않는다.

| 경과 시간 | 작업 | 이 구간의 산출물 |
| --- | --- | --- |
| 0~10분 | 심사 요건, 자격 증명 존재, 크레딧·용량, 비용·리전 승인, 공식 API·스키마·준비 이미지 확인 | 고정 본문·3개 예비 문항, 실제 연동 가능 여부, 스택 확정 |
| 10~25분 | 최소 서버·실행 환경 마련, Daytona 샌드박스와 Nosana GPU 작업 시작 | 실제 요청 식별자, 준비 상태, 키가 노출되지 않는 실행 경로 |
| 25~50분 | 소개 영역·본문·관찰 패널·SDK·별도 HTML 예제 구현 | 실제 스크롤과 유효 체류 집계가 보이는 사이트 |
| 50~70분 | 문제 제공·서버 채점·진행 잠금·재도전·초기화 연결 | 예비 문항으로 끝까지 작동하는 데모 |
| 70~90분 | Nosana 생성 결과 검수·연결, Daytona에서 최종 SDK 테스트·스모크, 증빙 수집 | 두 플랫폼의 실제 결과가 제품 흐름에 연결됨 |
| 90~105분 | 핵심 자동 테스트, 데스크톱·모바일 화면과 키보드 검증, 오류 수정 | 핵심 흐름 및 저장소 품질 게이트 결과 |
| 105~120분 | 공개 URL 확인, 90초 발표 리허설·녹화, 증빙 보존과 불필요한 작업 정리 | 심사자가 열 수 있는 사이트와 백업 데모 |

- 25분까지는 두 플랫폼의 요청을 시작한다. GPU가 준비되면 표의 순서를 기다리지 않고 생성 결과를 먼저 확보한다.
- 70분에는 예비 문항만으로도 체험이 끝까지 동작해야 한다. 플랫폼 대기 때문에 퀴즈 UI 구현을 멈추지 않는다.
- 90분부터 기능을 추가하지 않는다. 성공한 원격 결과를 보존하고 핵심 오류 수정·증빙·발표에 집중한다.
- 이 일정은 자격 증명과 사용 가능한 이미지·시장에 접근할 수 있다는 전제다. 준비 시간이 길어지면 P1을 버리며, 플랫폼 미완료는 숨기지 않는다.

## 8. 장애와 대체 경로

| 상황 | 대응 | 화면·발표의 표시 |
| --- | --- | --- |
| Nosana 첫 실행 대기·실패 | 작성된 3문항으로 체험 개발을 이어가고 제한된 시간 내 원인을 확인 | `샘플 문항 / Nosana 미연동`. 플랫폼 완료 기준은 미충족 |
| Nosana 생성 성공 후 종료·연결 실패 | 저장한 실제 생성 결과로 동일 본문 퀴즈 제공 | `Nosana 생성 결과 재사용`, 생성 시각과 현재 종료·오류 상태 |
| 생성 내용이 틀리거나 형식이 잘못됨 | 배포하지 않고 재생성·검수. 기한 내 해결되지 않으면 출처를 바꿔 샘플 모드로 제공 | 실제 문항 출처를 그대로 표시 |
| Daytona가 처음부터 실행되지 않음 | 로컬에서 동일 테스트를 수행해 제품 개발 유지 | `로컬 검증 / Daytona 미연동`. 플랫폼 완료 기준은 미충족 |
| Daytona 실행 후 샌드박스 만료 | 외부에 보존한 결과와 캡처로 실행 사실 제시 | 마지막 성공 시각과 만료 상태를 함께 표시 |
| 공개 호스팅·발표 네트워크 문제 | 로컬 데모와 미리 녹화한 실제 실행 화면 사용 | 녹화·로컬 실행임을 명시하고 공개 URL 실패를 알림 |

대체 경로는 체험 중단을 줄이기 위한 것이며 플랫폼 사용 점수를 대신 보장하지 않는다. 자격 증명이 없으면 값을 대화로 요청하지 않고 사용자가 안전한 실행 환경에 직접 설정하도록 한다.

## 9. 검증과 90초 발표

### 최소 검증

- 빠르게 넘긴 문단이 출제되고, 충분한 신호가 있는 문단은 제외된다. 숨김 탭에서 시간이 늘어나지 않는다.
- 오답·일부 정답·API 오류 때 다음 단계가 거부되고, 전체 정답 뒤에만 성공한다. 직접 다음 단계 API를 호출해도 같은 조건을 적용한다.
- 잘못된 문항 ID·다른 세션·중복 답변을 처리하고, 정답 데이터가 정적 파일·초기 API 응답·브라우저 번들에 노출되지 않는다.
- 마우스 없이 키보드와 터치로 완료할 수 있고, 다시 체험 후 이전 상태나 중복 이벤트가 남지 않는다.
- Playwright로 데스크톱·모바일 크기에서 실제 클릭 흐름과 스크린샷을 확인한다. 텍스트 넘침·가로 스크롤·버튼 겹침을 수정한다.
- 공급자 API 모킹으로 타임아웃·제한된 재시도·인증 실패·정리·멱등성·부분 배포 실패를 확인한다. 라이브 검증은 자격 증명과 비용 승인 후에만 한다.
- 도입한 도구의 포매터·린터·타입 검사·핵심 테스트·컨테이너 스모크를 실행한다. 환경상 못 한 검증은 성공으로 표시하지 않는다.
- 실제 결과 보존, 로그 비밀 제거, 작업 종료·삭제 또는 유한 유지 시간을 확인한다.

### 발표 순서

| 시간 | 보여줄 내용 |
| --- | --- |
| 0~15초 | 서비스 한 줄 소개와 실제 SDK 설치 코드 |
| 15~40초 | 글을 빠르게 넘기고 문단별 확인 필요 상태와 퀴즈 등장 시연 |
| 40~60초 | 오답으로 잠금 유지, 정답으로 다음 단계 진행 |
| 60~80초 | Nosana 생성 문항의 실행 출처와 Daytona SDK 테스트 증빙 |
| 80~90초 | 별도 HTML에서도 같은 SDK가 동작함을 보여주고 현재 한계 한 문장 설명 |

## 10. 시작 전에 확인할 사항

1. 두 플랫폼의 자격 증명과 크레딧이 준비되어 있는가? 허용 예산·실행 시간·데이터 처리 지역은 무엇인가?
2. 대회는 어떤 사용 증빙을 요구하는가? 재사용된 실제 생성 결과를 인정하는지, 심사 중 실시간 호출이 필수인지 확인한다.
3. 사용할 수 있는 고정 추론 이미지와 모델의 라이선스·요구 자원·현재 GPU 가용량은 확인했는가?
4. 심사자가 접속할 공개 URL을 어디서 제공하며, 발표 후 언제 자원을 종료하는가?

나머지는 이 문서의 고정 데모 범위를 기본으로 진행한다. 권한·비용·리전처럼 승인에 영향을 주는 결정만 구현 전에 확인하며, 모든 상용 요구 사항을 먼저 확정하려고 시간을 쓰지 않는다.